import "liblemon/io.lime"
import "liblemon/string.lime"

Token: struct {
    type: u32;
    text: String;
    indent: u32;
    line: u32;
    col: u32;
}

main: fn() -> i32 {
    let lol = String { length: 0, data: 0 };
    putstr(lol&);
    0
}
