from "liblemon/vec.lime" import Vec; // ??
use vec::Vec; // ??

import "liblemon/io.lime"

main: proc(argc: i32, argv: Vec<String>) = {
    let cmd = argv[0];
    match cmd {
        "new" | "init" {
            putstr("Do you want to use orange or a makefile to build your project?\n1) orange\n2) makefile");
            let response = io::stdin::readline();
            
        }
    }
}
