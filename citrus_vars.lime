fn add(a: i64, b: i64) -> i64 = a + b;

fn add_maybe(a: i64, b: i64) -> i64 {
    putd(a);
    putd(b);
    let added: i64 = add(a, b);
    let subbed = sub(a, b);
    let mulled: i64;
    mulled = mul(a, b);
    let divved;
    divved = div(a, b);
    
    added + subbed + mulled + divved
}
fn main() -> i64 {
    putd(add_maybe(69, 420));
    0
}
